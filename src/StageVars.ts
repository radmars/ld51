import { constants } from './constants';

export const StageVars = {
  initialGerms: constants.initialGerms,
  maxInitialGermSpeed: constants.maxInitialGermSpeed,
  spawnChance: constants.spawnChance,
};
